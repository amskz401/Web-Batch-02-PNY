import React from "react";

export default function Ifelse() {
  const val = 10;
  if (val == 10) {
    return <div>returning value {val}</div>;
  } else {
    return <div>returning value {val}</div>;
  }
}
